package com.example.livestockmanagement;

import android.os.Bundle;

import androidx.appcompat.app.AppCompatActivity;
/*
* Assign Tasks to farm employees,
set deadlines, track progress
* */

public class TasksTrackingActivity extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState){
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_tasks_tracking);
    }
}
